define(["exports", "module"], function (exports, module) {
    "use strict";

    var config = (function () {
        return {
            url: "http://localhost:8080/"
        };
    })();

    module.exports = config;
});